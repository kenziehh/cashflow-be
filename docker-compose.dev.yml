services:
  app:
    image: secure-cashflow-be-prod:latest
    container_name: cashflow-be-staging
    ports:
      - "${APP_PORT:-8081}:8081"
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - cashflow_staging_app_network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    container_name: cashflow-db-staging
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-password}
      - POSTGRES_DB=${DB_NAME:-cashflow_db}
    ports:
      - "${DB_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/migrations/:/docker-entrypoint-initdb.d/
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cashflow_staging_app_network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: redis-cashflow-staging
    ports:
      - "${REDIS_PORT:-6380}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cashflow_staging_app_network
    restart: unless-stopped

networks:
  cashflow_staging_app_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
